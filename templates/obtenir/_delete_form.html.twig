<form method="post" action="{{ path('app_obtenir_delete', {'id': obtenir.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ obtenir.id) }}">
    <button class="btn">Delete</button>
</form>
