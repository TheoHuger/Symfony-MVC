{% extends 'base.html.twig' %}

{% block title %}Activité{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1 class="text-center">Choisissez votre activité</h1>

    <div class="d-flex justify-content-center gap-3 mt-4">
        <a href="{{ path('app_ceinture_index') }}" class="btn btn-primary btn-lg">Ceinture</a>
        <a href="{{ path('app_adherent_index') }}" class="btn btn-success btn-lg">Adherent</a>
        <a href="{{ path('app_categorie_index') }}" class="btn btn-danger btn-lg">Categorie</a>
    </div>
</div>
{% endblock %}
