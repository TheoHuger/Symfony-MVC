{% extends 'base.html.twig' %}

{% block title %}Edit Ceinture{% endblock %}

{% block body %}
    <h1 class="text-center">Edit Ceinture</h1>

    {{ include('ceinture/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_ceinture_index') }}"class="btn btn-danger btn-min">back to list</a>

    {{ include('ceinture/_delete_form.html.twig') }}
{% endblock %}
